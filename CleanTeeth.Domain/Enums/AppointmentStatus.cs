namespace CleanTeeth.Domain.Enums;

public enum AppointmentStatus

    //deuda tecnica si no pongo 1,2,3,4, ya que si no lo hago agrego otro estado se dana todo
{
    Scheduled = 1,
    Cancelled = 2,
    Completed = 3,
}
